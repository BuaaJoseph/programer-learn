{
  "name": "llm-handbook",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server/index.js",
    "server:dev": "node --watch server/index.js",
    "db:init": "node server/scripts/init-db.js",
    "build": "npm run db:init && vite build",
    "preview": "vite preview",
    "build:app": "CAP_BUILD=1 vite build",
    "app:sync": "npm run build:app && cap sync",
    "app:android": "npm run build:app && cap sync android && cap open android",
    "app:ios": "npm run build:app && cap sync ios && cap open ios"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^9.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
