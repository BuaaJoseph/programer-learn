import Lead from '@/components/cards/Lead.jsx'
import KeyIdea from '@/components/cards/KeyIdea.jsx'
import Example from '@/components/cards/Example.jsx'
import Practice from '@/components/cards/Practice.jsx'
import Callout from '@/components/cards/Callout.jsx'
import CodeBlock from '@/components/cards/CodeBlock.jsx'
import Summary from '@/components/cards/Summary.jsx'
import CapabilityMultiplier from '@/courses/agent-internals/illustrations/CapabilityMultiplier.jsx'

const chatVsAgentCode = `# 同一个模型，两种「身体」

# A. 聊天框里：你问，它答，仅此而已
你：帮我给这个 API 加上 OAuth
模型：可以，大致思路是这样……（贴出一段示例代码）
# —— 它看不到你的代码，跑不了测试，改不了文件，到此为止

# B. Claude Code 里：同一个模型，套了一层脚手架
模型 -> Grep('def login')           # 工具：先找到登录相关代码
模型 -> Read('auth/routes.py')      # 工具：把文件读进上下文
模型 -> Edit('auth/routes.py', ...) # 工具：真的改文件
模型 -> Bash('pytest tests/auth')   # 工具：跑测试验证
模型 -> Edit(...)                   # 测试失败，根据报错再改
模型 -> Bash('pytest tests/auth')   # 再验证，通过
模型：OAuth 已接好，测试全绿。      # 没有 tool_use 了，停下交回给你`

const leversCode = `脚手架(harness)的四大杠杆

1. 工具设计     给模型一双手：Read / Edit / Bash / Grep …
2. 上下文工程   决定每一轮往模型眼前喂什么、喂多少
3. 规划与验证   先列计划，再动手，做完跑测试自我纠错
4. 提示工程     system prompt 规定它的身份、边界与工作方式

  能力 = 模型  ×  (工具 · 上下文 · 验证 · 提示)
                \\________ 脚手架，是个乘法器 ________/`

const sameModelCode = `# 一个心智实验：把模型权重冻住，只动外面那层

模型权重         = 完全一样（同一个 Claude）
─────────────────────────────────────────
产品 A：聊天框
  工具           = 无
  上下文         = 你粘贴进去的几段文字
  验证           = 无（它没法跑任何东西）
  → 表现：只能给建议，对错你自己判断

产品 B：编程 Agent
  工具           = Read / Edit / Bash / Grep / Glob …
  上下文         = 整个代码库（按需读进来）
  验证           = 跑测试、读报错、自我纠错
  → 表现：能把功能从零做到测试全绿

# 同一颗大脑，换了身体，输出能力差出一个数量级`

const harnessAnatomyCode = `# 把"脚手架"这个词拆开：它到底是哪几段代码？

harness/
├── tools/            # 杠杆1：每个工具的定义、参数校验、执行逻辑
│   ├── read.py
│   ├── edit.py
│   └── bash.py       #   危险命令在这里被拦下来问你
├── context/          # 杠杆2：决定每轮喂什么、超了怎么压缩
│   ├── assemble.py   #   把 system+历史+工具结果拼成一次请求
│   └── compact.py
├── loop/             # 杠杆3：那个"有 tool_use 就继续"的主循环
│   └── agent_loop.py
└── prompts/          # 杠杆4：system prompt、工具说明文案
    └── system.md
# —— 注意：以上没有一行是"模型"。脚手架全是普通工程代码。`

export default function Ch1() {
  return (
    <>
      <Lead>
        <p>
          有一件事会让很多人吃惊：在聊天框里只会「给建议」的那个 Claude，和在 Claude Code 里能改一整天代码、
          把功能从零做到测试全绿的那个 Claude，<strong>是同一个模型</strong>。底层权重一模一样，没有谁更聪明。
          差距全部来自模型外面那层叫<em>脚手架</em>（harness）的东西。本章就把这层东西拆开给你看。
        </p>
      </Lead>

      <h2>一个等式：Agent = 模型 + 脚手架</h2>
      <p>
        我们平时说的「Agent」，并不是某个特别强的模型，而是一个组合：中间是一个普通的大语言模型（LLM），
        外面包了一圈代码——这圈代码 Anthropic 在工程博客里叫它 <em>harness</em>，中文一般译作「脚手架」。
        脚手架负责给模型一双手（工具）、给它一双眼睛（上下文）、给它一套工作流程（规划与验证），
        还给它定身份和规矩（提示）。
      </p>
      <p>
        换句话说：<strong>模型负责「想」，脚手架负责把「想」变成「能做的事」。</strong>
        一个再聪明的大脑，如果没有手、没有眼睛、不能动，也只能空想。脚手架就是给这个大脑接上身体。
        这也是为什么同一个模型在不同产品里表现天差地别——不是模型变了，是它的「身体」变了。
      </p>
      <p>
        为什么要从这个等式讲起？因为它决定了你优化 Agent 的整个思路方向。如果你认定「Agent 的强弱
        约等于模型的强弱」，那你能做的只有等下一代模型、或者多花钱用更贵的模型——这是一条<strong>你几乎使不上劲</strong>
        的路。但如果你认清「Agent = 模型 × 脚手架」，那等式右边有一大半是<strong>你能亲手改写</strong>的工程代码。
        本课接下来的所有内容，本质上都是在教你怎么把等式右边那个「脚手架」因子调大。
      </p>

      <Example title="同一个 Claude，给 API 加 OAuth">
        <p>
          假设你想给一个后端 API 加上 OAuth 登录。把同样一句话分别丢给聊天框和 Claude Code，
          你会看到两件完全不同的事情：
        </p>
        <CodeBlock lang="text" title="chat-vs-agent" code={chatVsAgentCode} />
        <p>
          聊天框里它只能<strong>说</strong>怎么做——因为它看不到你的代码库、不能读文件、不能跑测试、改不了任何东西。
          Claude Code 里它能<strong>做</strong>——因为脚手架给了它 <code>Read</code>、<code>Edit</code>、
          <code>Bash</code>、<code>Grep</code> 这些工具，还给了它「改完跑测试、失败就再改」这套循环。
          模型的智商没变一分，变的是它能伸手够到的东西。
        </p>
        <p>
          再看一眼 B 路径的最后一行：模型说完「测试全绿」之后<strong>就停了</strong>。它不是被人喊停的，
          而是它自己判断「没有下一个动作要做了」，于是不再发出工具调用——这个停止信号本身也是脚手架在解读的。
          这个细节下一章会展开，这里先埋个伏笔：脚手架不只给模型手和眼，还负责<strong>翻译模型的意图</strong>。
        </p>
      </Example>

      <CapabilityMultiplier />

      <h2>脚手架到底是哪几段代码</h2>
      <p>
        「脚手架」这个词听起来有点玄，很多人第一次听会以为它是某种特殊的 AI 模块。恰恰相反——
        <strong>脚手架里几乎没有一行是 AI</strong>，全是再普通不过的工程代码：解析模型输出、校验参数、
        执行命令、把结果拼回去、超容量了做压缩、危险操作弹个确认。把目录摊开看，它长这样：
      </p>
      <CodeBlock lang="text" title="harness-anatomy" code={harnessAnatomyCode} />
      <p>
        看清这一点很重要，因为它解释了一个反复出现的误区：当 Agent 表现不好，人们的第一反应总是
        「模型不够强」。但你看上面这棵目录树——工具描述写得含糊、上下文拼装得乱、主循环没接验证、
        system prompt 漏了关键约束，<strong>任何一处工程缺陷</strong>都会让一个顶尖模型表现得像个二流模型。
        而这些恰恰都是脚手架的范畴，是你能改的。
      </p>

      <h2>脚手架的四大杠杆</h2>
      <p>
        Anthropic 在工程文章里把脚手架的发力点归纳为四类。把它们想成四根杠杆，每一根都能撬动同一个模型的实际能力：
      </p>
      <h3>1. 工具设计（给它手）</h3>
      <p>
        工具就是模型能调用的动作：读文件、改文件、执行命令、搜索代码。工具设计得好不好，直接决定模型能干多少活。
        一个描述清晰、参数合理、报错友好的工具，模型用起来又准又稳；一个含糊不清的工具，模型会反复试错、浪费回合。
        后面整整一卷都在讲工具怎么设计，这里先记住：<strong>有没有手、手好不好用，是第一道分水岭。</strong>
      </p>
      <p>
        底层原理是：模型并不能「直接」操作世界，它只会输出文本。所谓「调用工具」，无非是模型按约定格式吐出
        一段结构化文本（<code>{'tool_use'}</code> 块），脚手架解析后<strong>替它</strong>去执行真实动作，再把结果以文本喂回去。
        所以工具的「描述」对模型而言就是说明书——说明书写得越准，模型选工具、填参数的准确率就越高。
        一个常见的真实教训：把工具的报错信息写成对模型友好的「下一步该怎么办」，比写成冷冰冰的栈追踪，
        能显著减少它卡死或乱试的次数。这就是为什么工程经验里有句话——<strong>工具的报错文案也是提示工程</strong>。
      </p>
      <h3>2. 上下文工程（给它眼睛）</h3>
      <p>
        模型每一轮能「看见」的，只有当前喂进上下文窗口的那些字。喂什么、喂多少、什么时候清理，
        是脚手架最核心的活儿。Anthropic 有一个很反直觉但被反复验证的观察：
        <strong>多 Agent 系统之所以有效，很大程度上是因为它们能花掉足够多的 token</strong>。
        在一个叫 <em>BrowseComp</em> 的评测里，token 用量大约能解释 80% 的性能方差——
        也就是说，「往模型眼前铺了多少信息、让它做了多少步」比模型本身的差异更能预测最终表现。
        （以上为 Anthropic engineering 文章给出的数据）
      </p>
      <p>
        这里有个容易走偏的地方：「能花掉足够多 token」<strong>不等于</strong>「把上下文塞满就好」。
        恰恰相反，上下文越长，模型越容易读不准（第三章会讲到的 context rot）。正确理解是：
        <strong>让模型有机会做足够多步去收集到对的信息</strong>，而不是一股脑把所有文件全倒进去。
        这两者的区别，是「会用上下文」和「滥用上下文」的分水岭。
      </p>
      <h3>3. 规划与验证循环（给它工作流程）</h3>
      <p>
        强 Agent 不会一上来就乱改，而是先收集信息、列个计划，动手之后还会跑测试、读输出来验证对不对，错了再回头改。
        这套「行动—验证—纠错」的循环，是脚手架灌进去的纪律，不是模型自带的。下一章我们会把这个主循环整个走一遍。
      </p>
      <p>
        为什么验证这一环如此关键？因为模型生成代码时<strong>没有「执行」概念</strong>——它写出的代码在它脑子里是不是对的，
        它并不真的知道，直到有人真去跑一遍。验证循环做的就是把「真去跑一遍」接进流程：测试失败的报错，
        会变成下一轮的上下文，模型据此修正。没有验证环节的 Agent，相当于一个从不编译就交付的程序员——
        看起来写得飞快，但你永远不敢直接用它的产出。
      </p>
      <h3>4. 提示工程（给它身份和规矩）</h3>
      <p>
        system prompt 告诉模型「你是谁、你能碰什么、不能碰什么、按什么风格干活」。同一个模型，
        换一套 system prompt，行为可以截然不同。提示工程是四根杠杆里最轻、却往往最先被调的一根。
      </p>
      <p>
        但要小心一个常见误区：<strong>把提示当成万能补丁</strong>。当 Agent 老是犯某个错，很多人下意识就往
        system prompt 里再加一句「请务必不要……」。短期可能管用，长期却会把提示堆成一坨谁也读不懂的禁令清单，
        而且彼此打架。工程上的经验是：<strong>能用工具约束的，别用提示去求；能用流程保证的，别靠嘴提醒。</strong>
        比如「不许改 production 配置」，与其在提示里反复叮嘱，不如在工具层直接把那个目录设成只读——
        提示靠模型「自觉」，工具是硬约束，后者可靠得多。
      </p>
      <CodeBlock lang="text" title="four-levers" code={leversCode} />

      <KeyIdea title="脚手架是乘法器，不是加法器">
        <p>
          这四根杠杆不是简单地「加」在模型上，而是<strong>乘</strong>上去。一个普通模型配上一套好脚手架，
          实际能力可以翻很多倍；反过来，把顶尖模型塞进一个糟糕的脚手架里，它一样寸步难行。
          所以当你的 Agent 表现不好，第一反应不该是「换个更强的模型」，而是问：
          它的工具够不够用？上下文喂对了吗？有没有验证环节？提示写清楚了吗？
        </p>
        <p>
          乘法的另一面是：<strong>任何一根杠杆趋近于零，整体就趋近于零</strong>。工具再全，但上下文喂得一团糟，
          模型看不到关键信息，照样干不成事；验证循环再完整，但工具描述含糊到模型选错工具，验证也救不回来。
          这就是为什么优化 Agent 要找<strong>最短的那块板</strong>，而不是把已经够用的那根杠杆继续往死里调。
        </p>
      </KeyIdea>

      <p>
        把这个等式量化一下会更直观。下面是一张极度简化的对照表，数字只是示意、帮助建立直觉，
        重点是看清「同一个模型乘以不同脚手架」会拉开多大的差距：
      </p>
      <table>
        <thead>
          <tr>
            <th>配置</th>
            <th>模型</th>
            <th>工具</th>
            <th>上下文</th>
            <th>验证</th>
            <th>实际可用度</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>裸聊天框</td>
            <td>顶尖</td>
            <td>无</td>
            <td>靠粘贴</td>
            <td>无</td>
            <td>只能给建议</td>
          </tr>
          <tr>
            <td>半成品 Agent</td>
            <td>顶尖</td>
            <td>有但描述含糊</td>
            <td>乱塞</td>
            <td>无</td>
            <td>能动手但不可信</td>
          </tr>
          <tr>
            <td>成熟 Agent</td>
            <td>中档</td>
            <td>清晰好用</td>
            <td>按需精准</td>
            <td>跑测试自纠</td>
            <td>能独立交付</td>
          </tr>
        </tbody>
      </table>
      <p>
        注意第二行和第三行：第三行的<strong>模型档位更低</strong>，但因为脚手架到位，实际可用度反而最高。
        这正是「乘法器」最有力的证据——脚手架的增益，足以盖过一档模型的差距。
      </p>

      <Callout variant="warn" title="不是模型更聪明，是脚手架更强">
        <p>
          很多人会把 Claude Code 的强大归功于「它背后一定是个更厉害的模型」。这往往是误判。
          公开资料反复指向同一个结论：聊天框和编程工具用的常常是同一档模型，拉开差距的是外面那层脚手架。
          这件事对工程师是个好消息——因为脚手架是<strong>你能动手改的部分</strong>：你改不了模型权重，
          但你能设计工具、能组织上下文、能加验证循环。能力的增量，主要握在脚手架手里。
        </p>
      </Callout>

      <Example title="一个模型，三种脚手架">
        <p>
          为了让「乘法器」更可感，再看一个对照：把同一个模型权重冻住，只换它外面那层。
        </p>
        <CodeBlock lang="text" title="same-model-three-bodies" code={sameModelCode} />
        <p>
          产品 A 和产品 B 的模型字节完全相同，但 A 只能给建议、B 能交付功能。如果你只盯着「模型」这一列，
          永远解释不了这个差距——差距全写在下面那几行脚手架配置里。下次有人说「这个 Agent 不行，等更强的模型吧」，
          你可以反问一句：<strong>是模型不行，还是我们给它配的身体不行？</strong>
        </p>
      </Example>

      <h2>边界情况：什么时候「换模型」才是对的</h2>
      <p>
        前面一直在说「先别急着换模型」，但这不等于模型永远不重要——那是另一个极端的误区。
        脚手架是乘法器，可模型仍是被乘的那个基数：基数太小，乘多少都有限。
        什么情况下问题真的出在模型本身、换模型才是正解？大致有这么几类边界：
      </p>
      <ul>
        <li>
          <strong>能力天花板型</strong>：任务要求的推理深度、上下文长度、特定语言/领域知识，
          已经超出当前模型的硬上限。这时再优化工具和提示，只是在天花板下面打转。
        </li>
        <li>
          <strong>指令遵循差到无法约束</strong>：有些较弱的模型连「只输出 JSON」这种基本格式约束都频繁违反，
          脚手架要花大量代码去兜底。换一个指令遵循更好的模型，往往比写一堆兜底代码划算。
        </li>
        <li>
          <strong>成本/延迟错配</strong>：用顶尖大模型去干「总结一段旧对话」这种粗活，是浪费——
          这恰恰是该<strong>换小模型</strong>的场景（第三章会看到自动压缩就用轻量模型干这种活）。
        </li>
      </ul>
      <p>
        所以正确的判断顺序是：<strong>先排查四根杠杆有没有明显短板，确认脚手架没问题之后，再考虑模型</strong>。
        绝大多数「Agent 不好用」的抱怨，卡在前一步就能解决；只有少数真正撞到模型天花板的，才轮得到换模型。
      </p>

      <h2>这对做 Agent / 用 Agent 意味着什么</h2>
      <p>
        如果你在<strong>做</strong> Agent：把精力放在四根杠杆上，而不是一味追新模型。先把工具设计清楚、
        把上下文管好、把验证循环搭起来——这些带来的回报，往往比换模型更大、更可控。
      </p>
      <p>
        如果你在<strong>用</strong> Agent（比如天天用 Claude Code 或 Cursor）：理解了脚手架的存在，
        你就知道为什么「让它先看一眼相关文件再动手」「让它跑完测试再说完成」这类操作能显著提升效果——
        你其实是在帮脚手架把上下文喂对、把验证补上。会用 Agent 的人，本质上是懂得怎么配合那层脚手架的人。
      </p>
      <p>
        换个角度说，作为使用者，你自己也是脚手架的一部分。你给的指令在塑造它的提示，你让它先读哪些文件在塑造它的上下文，
        你要求它「测完再说完成」在替它补验证。<strong>一个会用 Agent 的人，等于临时给模型加装了一截人肉脚手架</strong>——
        这也是为什么同一个 Claude Code，在不同人手里产出质量能差出一大截。
      </p>

      <Practice title="给你常用的 Agent 做一次「三件套」体检">
        <p>
          挑一个你每天在用的 Agent（Claude Code、Cursor，或别的），用一页纸列出它的脚手架三件套：
        </p>
        <ul>
          <li><strong>工具</strong>：它能调用哪些动作？读文件、改文件、跑命令、联网搜索……分别叫什么？</li>
          <li><strong>记忆</strong>：它靠什么记住跨轮、跨会话的信息？是 CLAUDE.md、规则文件，还是别的机制？</li>
          <li><strong>验证</strong>：它做完一件事后，怎么确认做对了？跑测试、读输出、还是全靠你人工检查？</li>
        </ul>
        <p>
          列完之后，找出那个最弱的环节——通常它就是你这个 Agent 表现忽好忽坏的根源。
          这份清单也会成为后面几卷的对照表：每学一个主题，就回来看看自己的 Agent 在那一项上能不能补强。
        </p>
        <p>
          进阶一步：回想你最近一次「Agent 表现很差」的经历，对照四根杠杆给它归因——
          到底是工具不趁手、上下文喂错了、没验证，还是提示没写清？你会发现，绝大多数时候
          <strong>根本轮不到「模型不行」这一条</strong>。把这个归因习惯练出来，你排查 Agent 问题的效率会高一大截。
        </p>
      </Practice>

      <Summary
        points={[
          'Agent = 模型 + 脚手架（harness）：聊天框里只会给建议、Claude Code 里能改一整天代码，背后常是同一个模型，差距全在脚手架。',
          '脚手架几乎不含 AI：它就是工具定义、上下文拼装、主循环、提示文案这些普通工程代码——也正因如此，它是你能动手改的部分。',
          '脚手架四大杠杆：工具设计（给手）、上下文工程（给眼睛）、规划与验证循环（给流程）、提示工程（给身份和规矩）。',
          '上下文是核心：Anthropic 指出多 Agent 有效很大程度因为能花掉足够多 token，BrowseComp 评测里 token 用量约能解释 80% 性能方差——但这是"让它多走几步收集对的信息"，不是"把窗口塞满"。',
          '脚手架是乘法器不是加法器：好脚手架能把普通模型翻倍，烂脚手架能让顶尖模型寸步难行；任一杠杆趋零整体趋零，要找最短的板。',
          '能力增量主要握在脚手架手里——模型权重你改不了，但工具、上下文、验证、提示都是你能动手改的部分。',
          '换模型也有对的时候：撞到能力天花板、指令遵循差到无法约束、或成本延迟错配（该用小模型却用大模型）——但要先排完脚手架短板再考虑。',
          '表现不好先别急着换模型，先体检脚手架的「工具 / 记忆 / 验证」三件套，补最弱的那一环；使用者本身也是一截"人肉脚手架"。',
        ]}
      />
    </>
  )
}
