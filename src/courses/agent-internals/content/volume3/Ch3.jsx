import Lead from '@/components/cards/Lead.jsx'
import KeyIdea from '@/components/cards/KeyIdea.jsx'
import Example from '@/components/cards/Example.jsx'
import Practice from '@/components/cards/Practice.jsx'
import Callout from '@/components/cards/Callout.jsx'
import CodeBlock from '@/components/cards/CodeBlock.jsx'
import Summary from '@/components/cards/Summary.jsx'
import TaskCompare from '@/courses/agent-internals/illustrations/TaskCompare.jsx'

const tasksCode = `查 bug:   「修一下登录按钮点了没反应的问题」
加功能:   「给用户设置页加一个深色模式开关，记住选择」
大重构:   「把整个项目的状态管理从 Redux 迁移到 Zustand」`

const refactorPlanCode = `// 大重构里，Agent 往往先落一份可勾选的计划，再逐项推进
[
  { "step": 1, "task": "用子代理全仓库找出所有 useSelector/useDispatch 调用", "done": true },
  { "step": 2, "task": "设计 Zustand store 分片方案，请用户确认", "done": true },
  { "step": 3, "task": "迁移 auth 模块并跑测试", "done": false },
  { "step": 4, "task": "迁移 cart 模块并跑测试", "done": false },
  { "step": 5, "task": "删除 Redux 依赖与 Provider，全量回归", "done": false }
]
// 计划落在上下文里，等于给几十轮的循环装了一根「主心骨」，防止越改越偏`

const shapeCode = `// 用两个维度给任务打分，结果直接映射到该配多少脚手架
function scaffoldFor(task) {
  const { certainty, breadth } = task   // 各 0~1：越大越确定 / 越宽

  if (certainty > 0.7 && breadth < 0.3) return '直接干（几轮）'
  if (breadth > 0.7 || certainty < 0.3) return '规划+子代理+人审+压缩（几十轮）'
  return '轻量规划+可能问一次（约十轮）'
}`

export default function Ch3() {
  return (
    <>
      <Lead>
        <p>
          为什么同样一句指令，有的 Agent 一轮就交活，有的要折腾几十轮？不是模型时好时坏，而是
          <strong>任务本身的形状不同</strong>。这一章把三类最常见的开发任务——查 bug、加功能、大重构——
          的内部执行路径并排摆出来，你会看到：任务越大越开放，用到的脚手架机制就越多，轮数也越多。
        </p>
      </Lead>

      <h2>三类任务，三条路径</h2>
      <p>
        先看三个真实形态的指令，它们的「确定性」和「广度」差异极大：
      </p>
      <CodeBlock lang="text" title="三类任务的典型指令" code={tasksCode} />

      <h3>查 bug：范围小，几轮收敛</h3>
      <p>
        bug 通常有明确症状、明确落点。Agent 的路径很短：定位（读几个相关文件、搜一下报错）→ 改一处 → 验证。
        范围小、目标清晰，<strong>几轮就能收敛</strong>，一般<strong>不需要计划、也不需要子代理</strong>，
        更不用反复问你。这就是「直接干」的典型。
      </p>
      <p>
        但「查 bug」里藏着一个分叉，决定它是真的几轮收敛、还是会意外膨胀：<strong>症状到根因的距离</strong>。
        「点了没反应」如果根因就是按钮少绑了个事件，那确实三两轮搞定；可如果根因是某个状态在异步竞态下被覆盖，
        定位本身就成了一场调研——这时它已经在悄悄向「加功能」甚至「重构」滑动了。所以更准确的说法是：
        <strong>查 bug 的路径短，前提是根因离症状近</strong>。一旦发现要读十几个文件才摸到边，
        就该意识到任务形状变了，该升级脚手架（哪怕只是先让它写一句调查计划）。这正是很多人「修个小 bug 修了一下午」的真相：
        不是模型菜，是任务被错判成了小任务。
      </p>

      <h3>加功能：要规划，约十轮</h3>
      <p>
        加功能比修 bug 开放：要在已有结构里找合适的落点、可能动到好几个文件、改完还要验证。
        路径变长——通常会<strong>先做个轻量规划</strong>，<strong>可能问你一次</strong>（比如「开关默认开还是关」），
        然后多次修改与验证交替，<strong>大约十轮上下</strong>。它用到的脚手架比查 bug 多一层：规划。
      </p>
      <p>
        这里「轻量规划」四个字的分量常被低估。它的真正作用不是给你看的待办清单，而是<strong>把模型当下的决策固化进上下文</strong>，
        防止它在后续十轮里「忘了自己原本打算怎么做」。模型没有跨轮的记忆，它每一轮的判断只来自当前上下文；
        如果第一轮决定了「开关状态存 localStorage、用 context 透传」，却没写下来，到第六轮它完全可能改用别的方案、
        造成前后不一致。一句写在上下文里的计划，等于给这条十轮的链条钉了个锚。至于「问你一次」——
        好的 Agent 会把问题攒到<strong>真正影响走向的岔路口</strong>才问（默认开还是关、要不要兼容旧数据），
        而不是每一步都来确认；问得太碎和从不问，是两种相反的毛病。
      </p>

      <h3>大重构：规划 + 子代理 + 多次人审，几十轮</h3>
      <p>
        大重构是最开放的：影响面遍布全项目、充满不确定。它几乎会动用所有脚手架——
        <strong>先规划</strong>、派<strong>子代理去调研</strong>（全仓库找出所有受影响的地方，见上一章）、
        分批修改并<strong>多次请人审阅</strong>、过程中还要<strong>压缩上下文</strong>以免被海量中间信息撑爆，
        往往要跑<strong>几十轮</strong>。
      </p>
      <CodeBlock lang="json" title="大重构里的一份可勾选计划" code={refactorPlanCode} />
      <p>
        为什么大重构非得这么重？因为它同时踩中了前两章讲的所有痛点：影响面大意味着光是「找全受影响处」就得动用子代理（否则漏改）；
        轮数多意味着上下文必然膨胀，不压缩就会被中间产物撑爆、模型在噪音里迷路；不确定性高意味着任何一步走错都可能波及全局，
        所以要把关键节点切成「分批修改 + 跑测试 + 请人审」的小段，<strong>用频繁的验证把错误关在小范围里</strong>。
        一个常见误区是觉得「让 Agent 一口气重构完最省事」——恰恰相反，越是大重构，越要把它切碎、每段都能独立验证、
        每段都给人一个叫停的机会。重构的风险不在于改得慢，而在于<strong>错误被无声地累积到无法回滚</strong>。
      </p>

      <Example title="同一个动词「改」，三种执行路径">
        <p>把三类任务的内部路径并排看，差异一目了然：</p>
        <ul>
          <li>
            <strong>查 bug</strong>　轮数：几轮｜计划：否｜子代理：否｜问用户：基本不问｜上下文压缩：不需要
          </li>
          <li>
            <strong>加功能</strong>　轮数：约十轮｜计划：轻量｜子代理：通常不必｜问用户：可能一次｜上下文压缩：一般不需要
          </li>
          <li>
            <strong>大重构</strong>　轮数：几十轮｜计划：是｜子代理：是（调研）｜问用户：多次人审｜上下文压缩：需要
          </li>
        </ul>
      </Example>

      <TaskCompare />

      <KeyIdea title="确定性与广度决定脚手架">
        <p>
          决定一个任务跑几轮、用哪些机制的，是两个维度：<strong>确定性</strong>（目标和落点有多清楚）和
          <strong>广度</strong>（影响多少代码）。越确定、越窄，就越接近「直接干」；越开放、越宽，
          就越需要规划、子代理、人审、上下文压缩这些脚手架来兜住复杂度。轮数多不是浪费，是任务形状的必然结果。
        </p>
      </KeyIdea>

      <h3>把两个维度写成一张决策表</h3>
      <p>
        与其凭感觉，不如把「确定性 × 广度」摊成一张表，每一格直接对应该配的脚手架。它不是死规矩，而是帮你快速定位的脚手架本身：
      </p>
      <table>
        <thead>
          <tr>
            <th>　</th>
            <th>窄（影响少量文件）</th>
            <th>宽（影响全项目）</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>确定（目标落点清楚）</strong></td>
            <td>直接干，几轮｜典型：查 bug、改文案</td>
            <td>规划 + 分批改 + 测试兜底｜典型：统一替换某 API</td>
          </tr>
          <tr>
            <td><strong>不确定（要边做边探）</strong></td>
            <td>轻量规划 + 可能问一次｜典型：加功能</td>
            <td>全套脚手架，几十轮｜典型：架构级大重构</td>
          </tr>
        </tbody>
      </table>
      <p>
        注意右上角那一格——「确定但很宽」常被忽略：目标其实很明确（把 <code>oldFetch</code> 全换成 <code>newFetch</code>），
        但因为影响面大，仍然需要子代理去找全、需要分批改加测试兜底。可见<strong>广度单独一个维度就足以把任务推重</strong>，
        不必非得「又难又大」才用重脚手架。
      </p>
      <CodeBlock lang="javascript" title="把这张表写成一个判断函数" code={shapeCode} />

      <Callout variant="warn" title="给错脚手架两头都坏">
        <p>
          给小任务套大流程——让 Agent 为一个一行 bug 先写计划、再派子代理调研——是纯浪费，又慢又啰嗦。
          反过来，给大重构按小任务「直接干」，则极可能跑飞：没规划就乱改、不调研就漏掉受影响处、不请人审就把全项目带沟里。
          关键是<strong>给任务配相称的脚手架</strong>。
        </p>
      </Callout>

      <Callout variant="info" title="边界：任务形状会在执行中途变化">
        <p>
          形状不是开工前一次定死的。一个被判为「查 bug」的任务，做着做着发现根因牵动了三个模块——它当场升级成了重构。
          好的 Agent（和好的使用者）会<strong>在循环中途重新评估形状</strong>：察觉范围扩大了，就停下来补一份计划、
          甚至回头问你一句「这个改动会动到 X，要继续吗」，而不是闷头按原计划把小流程硬跑到底。
          把「形状判断」当成贯穿全程的动作，而非起点的一锤子买卖。
        </p>
      </Callout>

      <h2>这对你意味着什么</h2>
      <p>
        派活之前，先在心里给任务定个位：它确定吗？它宽吗？答案会直接告诉你该不该「先规划」。
        症状明确、落点单一的，放手让它直接干；要碰好几个文件、有取舍要定的，让它先简单规划、必要时问你一句；
        牵动全局、充满未知的，就别指望一口气吃完——明确要求它先规划、用子代理调研、并安排你来把关。
        <strong>你对任务形状判断得越准，Agent 表现得就越稳。</strong>
      </p>
      <p>
        把这套判断变成你下指令的习惯，收益是双向的。对宽而不确定的任务，你可以在指令里<strong>主动指定脚手架</strong>——
        「先列个迁移计划给我看，再分模块改，每改完一个跑测试」——等于替 Agent 把形状判断做在了前头，省掉它自己摸索的几轮。
        对窄而确定的任务，则反过来<strong>主动压制脚手架</strong>——「直接改，别写计划」——免得它过度郑重其事。
        说到底，这一卷讲的工具调度、子代理、规划压缩，都是手段；这一章给你的是那把<strong>「何时该上哪种手段」的尺子</strong>。
        尺子准了，前面的机制才用在刀刃上。
      </p>

      <Practice title="给你自己的三个任务归类并预测路径">
        <p>
          从你正在做的项目里挑三个真实任务，分别归入「查 bug / 加功能 / 大重构」，然后为每个预测它的执行路径：
        </p>
        <ul>
          <li>它大概要跑<strong>几轮</strong>？</li>
          <li>需要<strong>先规划</strong>吗？</li>
          <li>会用到<strong>子代理</strong>去调研吗？</li>
          <li>过程中<strong>该不该问你（或请你审）</strong>？几次？</li>
        </ul>
        <p>
          填完后回头检查：你的预测和「确定性 + 广度」这两个维度对得上吗？如果一个任务你说不准它属于哪类，
          那往往说明它<strong>还没拆够细</strong>——先把它拆成更确定的子任务再派。
        </p>
        <p>
          进阶一问：挑出你那个「查 bug」任务，假设做到一半发现根因远比预想的深、牵动了好几个文件，
          你会怎么<strong>中途调整脚手架</strong>？把这一步也写下来——这恰恰是新手最容易漏掉的判断。
        </p>
      </Practice>

      <Summary
        points={[
          '同一句指令轮数差很多，根源不是模型，而是任务形状：取决于它的确定性与广度。',
          '查 bug：范围小、目标清晰，几轮收敛，通常不需要计划、子代理或反复问用户——典型「直接干」；但前提是根因离症状近，根因一深它就会向加功能/重构滑动。',
          '加功能：较开放，需轻量规划、可能问一次、多次改与验证交替，约十轮；规划的真正作用是把决策固化进上下文、防止跨轮遗忘。',
          '大重构：最开放，几乎用上全部脚手架——规划、子代理调研、多次人审、上下文压缩，跑几十轮；要切碎、每段可验证，把错误关在小范围而非无声累积。',
          '可把「确定性 × 广度」摊成一张决策表：注意「确定但很宽」也需要重脚手架，广度单独就能把任务推重。',
          '任务越大越开放，用到的脚手架机制越多、轮数越多；给错脚手架（小任务套大流程或反之）两头都坏。',
          '任务形状会在执行中途变化，要持续重评估、随时升降脚手架，而非开工时一锤定音。',
          '派活前先判断任务的确定性与广度来决定「直接干」还是「先规划」，并可在指令里主动指定或压制脚手架；判断不准往往说明任务还没拆够细。',
        ]}
      />
    </>
  )
}
