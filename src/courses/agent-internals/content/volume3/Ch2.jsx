import Lead from '@/components/cards/Lead.jsx'
import KeyIdea from '@/components/cards/KeyIdea.jsx'
import Example from '@/components/cards/Example.jsx'
import Practice from '@/components/cards/Practice.jsx'
import Callout from '@/components/cards/Callout.jsx'
import CodeBlock from '@/components/cards/CodeBlock.jsx'
import Summary from '@/components/cards/Summary.jsx'
import SubagentIsolation from '@/courses/agent-internals/illustrations/SubagentIsolation.jsx'

const taskCallCode = `// 主代理派出一个子代理，只交代「任务 + 它需要的工具」
{
  "name": "Task",
  "input": {
    "description": "找出所有调用旧 API 的地方",
    "prompt": "在整个仓库里搜索对 oldFetch() 的调用，逐个确认是真实调用而非注释或字符串，最后只回传一份文件清单。",
    "subagent_type": "Explore"
  }
}

// 子代理在自己的上下文里读了几十个文件、跑了多轮搜索，
// 但回传给主代理的只有一段摘要：
{
  "type": "tool_result",
  "content": "命中 7 处：\\n- src/user.js:42\\n- src/order.js:88\\n- ..."
}`

const badVsGoodPromptCode = `// 反例：背景全靠子代理「自己猜」，结果它用了和项目不一致的风格
{
  "description": "审查这个 PR",
  "prompt": "看看 PR #128 有没有问题。"
}

// 正例：把隐含约束显式写进任务，子代理才接得住
{
  "description": "审查这个 PR",
  "prompt": [
    "审查 PR #128（给购物车加优惠券功能）。",
    "项目约定：所有金额用整数分存储，禁止用浮点；错误必须走统一的 AppError。",
    "重点看：金额计算是否有浮点、并发改库存有没有加锁、新接口有没有写测试。",
    "只回传一份按【严重/警告/建议】分级的问题清单，不要把 diff 复述一遍。"
  ].join("\\n")
}`

const fanOutCode = `// 主代理一轮里并行派出三个只读子代理，各查一块，互不干扰
[
  { "name": "Task", "input": { "subagent_type": "Explore",
    "prompt": "梳理鉴权模块的调用链，回传一张依赖图说明。" } },
  { "name": "Task", "input": { "subagent_type": "Explore",
    "prompt": "找出所有直接拼 SQL 的地方，回传文件:行号清单。" } },
  { "name": "Task", "input": { "subagent_type": "Explore",
    "prompt": "统计测试覆盖薄弱的目录，回传 top 5 目录。" } }
]
// 三份摘要回到主线，主代理再综合 —— 三块脏活的中间过程谁也没进主上下文`

export default function Ch2() {
  return (
    <>
      <Lead>
        <p>
          有些子任务很「脏」：要翻几十个文件、跑十几轮搜索、刷出一屏屏日志，可你最后只想要一句结论。
          如果这些中间过程全堆进主对话，主上下文很快就被噪音塞满。子代理（<em>Task</em> 工具）正是为此而生——
          它本质上是一个<strong>上下文隔离器</strong>：把脏活关在一个独立的房间里干完，只把干净的结论端出来。
        </p>
      </Lead>

      <h2>子代理是什么</h2>
      <p>
        子代理不是什么神秘的「第二个 AI」，它就是主代理通过 <code>Task</code> 工具发起的一次<strong>独立运行</strong>。
        它有四个关键特征：
      </p>
      <ul>
        <li><strong>独立上下文</strong>：它有自己的上下文窗口，从一张白纸开始，不背主对话的历史包袱。</li>
        <li><strong>自己的 system 与工具集</strong>：它有自己的系统提示、自己被授予的那一小撮工具。</li>
        <li><strong>只回传摘要</strong>：它处理完一个独立子任务后，只把<strong>最终结果</strong>作为 tool_result 回传父上下文，中间过程留在自己屋里。</li>
        <li><strong>可并行、可限权</strong>：主代理能同时派出多个子代理，且只给每个子代理它真正需要的工具。</li>
      </ul>
      <p>
        把它放回上一章的循环模型里看，会更透彻：<strong>子代理就是「一个工具调用」本身，只不过这个工具内部又跑了一整个 harness 循环</strong>。
        主代理产出一个 <code>{'tool_use'}</code>（名字叫 Task），harness 收到后不是去读文件，而是启动一个全新的、
        带独立 messages 数组的循环，让子代理在里面反复「意图—执行—回灌」转上几十圈；等子代理停了，
        harness 把它的最终输出包成<strong>一条</strong> <code>{'tool_result'}</code> 交还给主代理。
        所以从主代理的视角，派一个跑了 30 轮的子代理，和读一个文件，在上下文里留下的痕迹是一样的——都只是一问一答。
        这正是「隔离」二字的机制来源。
      </p>

      <Example title="派一个子代理去全仓库找旧 API 调用">
        <p>
          假设你要把 <code>oldFetch()</code> 全部换掉，第一步得先找全。这件事会翻遍仓库、读很多文件——典型的「脏活」，
          交给子代理最合适：
        </p>
        <CodeBlock lang="json" title="Task：找旧 API 调用" code={taskCallCode} />
        <p>
          子代理可能读了 30 个文件、做了多轮 Grep，但这些都不会进主上下文。主代理拿到的只是那份七行清单，
          干净、聚焦，正好用来规划下一步的修改。
        </p>
      </Example>

      <SubagentIsolation />

      <h2>为什么要用子代理</h2>
      <p>它的价值可以归到三点，且都直接对应上一章讲的「上下文是稀缺资源」：</p>
      <ul>
        <li>
          <strong>省主上下文</strong>：搜索、调研这类动作会产出大量中间垃圾，隔离在子代理里，
          主上下文就只留结论，能撑得更久、看得更清。
        </li>
        <li>
          <strong>聚焦</strong>：子代理只盯一个独立子任务，系统提示和工具都为它量身定制，干扰更少、更容易做好。
        </li>
        <li>
          <strong>隔离权限</strong>：可以只给子代理只读工具去调研，不给它写权限，把「能搜」和「能改」分开，更安全。
        </li>
      </ul>
      <p>
        Anthropic 自己描述这种模式时说得很直白：让详细的搜索、调研在子代理里进行，<strong>主代理只负责综合各路结果</strong>。
        主代理像项目负责人，子代理像被派出去的调查员——负责人不需要看调查员翻过的每一页纸，只需要那份结案报告。
      </p>
      <p>
        这里有个值得细想的底层原因：模型的注意力是<strong>有限且随上下文变长而稀释</strong>的。当主对话里堆了几万 token 的
        搜索中间结果，模型在后续推理时要在这片噪音里捞针，准确率会肉眼可见地下降（业界叫它「context rot」或
        「lost in the middle」）。子代理的隔离不只是「省钱省 token」，更是<strong>在保护主代理的注意力</strong>——
        让它始终面对一份精炼的、信噪比高的上下文。从这个角度看，子代理和后面要讲的上下文压缩是同一类武器：
        都在对抗「上下文越长、模型越糊涂」这个根本约束。
      </p>

      <h3>并行 fan-out：隔离的另一重红利</h3>
      <p>
        因为每个子代理都活在自己的上下文里、互不干扰，主代理可以在<strong>同一轮里并行派出好几个</strong>，
        让它们各查一块，最后把几份摘要一起收回来综合。这正好接上一章「只读可并行」的规则——
        调研型子代理通常只读不写，天然可交换、可并行。
      </p>
      <CodeBlock lang="json" title="一轮并行派出三个调研子代理" code={fanOutCode} />
      <p>
        这种「主代理拆任务、多个子代理并行调研、主代理再综合」的形状，是大型代码库探索里最划算的提速手段之一：
        三块脏活同时进行，墙上时间几乎只花一块的钱，而主上下文里只多了三条干净摘要。但要记住边界——
        <strong>只有彼此独立、且不写状态的子任务才适合这样 fan-out</strong>；若几个子代理要改同一片代码，
        就退回上一章的串行规则，否则结果会互相覆盖。
      </p>

      <KeyIdea title="子代理 = 上下文隔离器">
        <p>
          理解子代理只需记住一句话：它把「过程」和「结论」分开。过程（几十个文件、满屏日志）留在隔离的上下文里随它去，
          结论（一段摘要）才回到主线。它解决的核心问题不是「让 AI 更聪明」，而是
          <strong>不让主上下文被中间过程塞爆</strong>。
        </p>
      </KeyIdea>

      <Callout variant="warn" title="隔离是有代价的">
        <p>
          正因为子代理从白纸起步，它<strong>拿不到主上下文里的隐含约束</strong>——你在主对话里随口定下的命名规范、
          上一步刚发现的坑、项目特有的口味，子代理统统不知道。所以派活时要把它需要的背景<strong>显式写进任务描述</strong>，
          别指望它「自己懂」。隔离省下了上下文，代价就是这份信息不会自动流过去。
        </p>
      </Callout>

      <h3>把隐含约束写进任务：一个正反对比</h3>
      <p>
        上面那条警告很容易点头称是、落地却频频翻车。最典型的失败是：主代理嫌写任务描述麻烦，丢一句「审查这个 PR」就派出去，
        子代理在白纸状态下只能靠通识去审，给出一堆和你项目八竿子打不着的「建议」。对比一下两种派法：
      </p>
      <CodeBlock lang="json" title="反例 vs 正例：任务描述要不要带约束" code={badVsGoodPromptCode} />
      <p>
        正例多出来的那几行，恰恰是主代理上下文里有、而子代理上下文里没有的信息：项目的金额存储约定、统一错误处理的口味、
        本次改动真正该盯的风险点、以及对输出格式的明确要求。一个实用的心法是：派活前问自己一句——
        <strong>「如果换一个完全没看过这个项目的人来做，他会问我哪几个问题？」把那几个问题的答案，预先写进 prompt 里。</strong>
      </p>

      <h2>它不是「多 Agent 编排」</h2>
      <p>
        别把子代理想成那种复杂的多 Agent 协作系统——一堆 Agent 互相对话、协商、扮演不同角色。这里的子代理朴素得多：
        主代理派出去、子代理干完一个独立子任务、回传摘要，关系就结束了。它是一个<strong>工具</strong>，
        不是一套编排框架。把它当成「带隔离的一次性外包」来理解，恰到好处。
      </p>
      <p>
        这两者的差别值得说透，因为踩坑的人多。多 Agent 编排（multi-agent orchestration）里，Agent 之间会<strong>反复来回通信</strong>、
        共享中间状态、甚至互相纠正——这听起来很美，实际工程里却脆弱得很：通信轮次一多，错误会层层放大，
        一个 Agent 的幻觉被另一个当成事实采信，调试时几乎无从下手。而子代理刻意把关系做成<strong>单向、一次性、无状态</strong>的：
        派出去、拿回一段文字、结束。没有来回扯皮，就没有放大效应。这是一种「用约束换可靠」的克制设计，
        而不是能力不足。常见误区是看到「子代理」就联想到 AutoGPT 那类多角色互聊的玩法，从而高估了它的复杂度、也用错了它。
      </p>
      <Callout variant="info" title="边界：子代理不该被滥用">
        <p>
          隔离不是免费的。每派一个子代理，都要重新付一遍系统提示、工具定义、任务背景的 token，还要等它从零冷启动跑完整个循环。
          对那种「读一个文件就够」的小事，直接在主代理里读反而更快更省。子代理的甜区是
          <strong>「中间过程很多、最终结论很短」</strong>的任务——投入产出比才划算。如果一个子任务回传的摘要几乎和它读进去的一样长，
          那说明它根本没起到「隔离压缩」的作用，不如不派。
        </p>
      </Callout>

      <h2>这对你意味着什么</h2>
      <p>
        当你发现一个步骤会产生大量你最终并不关心的中间内容——全仓库搜索、读一大批文件、跑探索性命令——
        那就是动用子代理的信号。设计时问自己三件事：<strong>这个子任务是否独立</strong>？
        <strong>它最终只需回传什么摘要</strong>？<strong>它最少需要哪些工具</strong>？想清楚这三点，
        子代理就能既省上下文又安全。
      </p>
      <p>
        再补两条来自实战的经验。其一，<strong>把「输出格式」也当成任务的一部分写死</strong>——
        要求子代理回传「文件:行号清单」还是「分级问题列表」，直接决定了主代理拿到摘要后好不好用；
        含糊的输出会逼主代理再花一轮去理解。其二，<strong>给调研型子代理只读权限是默认正确的选择</strong>：
        让「找问题」和「改代码」分两步、由不同权限的角色完成，既符合最小权限原则，也让你有机会在子代理给出清单后、
        主代理动手前插一脚把关。
      </p>

      <Practice title="设计一个「代码审查子代理」">
        <p>
          你要派一个子代理去审查一个 Pull Request。请设计它，至少回答：
        </p>
        <ul>
          <li><strong>目标</strong>：它要产出什么？（提示：一份按严重程度分类的问题清单，而不是把 diff 重复一遍）</li>
          <li><strong>该给哪些工具</strong>：它需要 <code>Read</code>、<code>Grep</code> 这类只读工具吗？该不该给它 <code>Edit</code> 或 <code>Bash</code>？为什么？</li>
          <li><strong>该回传什么摘要</strong>：哪些中间过程应当留在子代理里、不回主上下文？</li>
          <li><strong>要显式交代哪些隐含约束</strong>：项目的代码规范、本次改动的关注点，怎样写进任务描述才不会丢？</li>
        </ul>
        <p>
          进阶：再想一想，如果这个 PR 很大、涉及鉴权和支付两个互不相干的模块，你会派<strong>一个</strong>审查子代理，
          还是并行派<strong>两个</strong>各审一块？两种做法对主上下文、对审查质量各有什么影响？
        </p>
      </Practice>

      <Summary
        points={[
          '子代理（Task 工具）本质是上下文隔离器：把会产生大量中间内容的独立子任务关进一个独立运行里。',
          '机制上，子代理就是「一个工具调用」，只不过其内部又跑了一整个 harness 循环；主代理只看到它回传的那一条 tool_result。',
          '它有自己的上下文窗口、自己的 system 与工具集，处理完只把最终结果摘要作为 tool_result 回传父上下文。',
          '用它的三大理由：省主上下文、聚焦单一子任务、按需限权（比如只给只读工具去调研）；更深层是保护主代理有限的注意力，对抗「上下文越长越糊涂」。',
          '只读调研型子代理可在一轮里并行 fan-out、各查一块再综合；但要写同一片状态的子任务必须退回串行。',
          'Anthropic 的用法是让详细搜索/调研在子代理里进行，主代理只综合结果，像负责人只看结案报告。',
          '代价：子代理从白纸起步，拿不到主上下文的隐含约束，必须把背景与输出格式显式写进任务描述。',
          '它不是复杂的多 Agent 编排（那种反复通信会放大错误），而是单向、一次性、无状态的工具；小事别滥用，甜区是「中间过程多、结论短」。',
        ]}
      />
    </>
  )
}
