import Lead from '@/components/cards/Lead.jsx'
import KeyIdea from '@/components/cards/KeyIdea.jsx'
import Callout from '@/components/cards/Callout.jsx'
import CodeBlock from '@/components/cards/CodeBlock.jsx'
import Example from '@/components/cards/Example.jsx'
import Summary from '@/components/cards/Summary.jsx'

const syncCallSnippet = `// 下单服务同步调用三个下游：串行、强耦合、慢
public OrderResult createOrder(OrderReq req) {
    Order order = orderDao.save(req);     // 1. 落库（必须成功）
    pointService.addPoints(order);        // 2. 加积分（慢，可能超时）
    smsService.sendSms(order);            // 3. 发短信（第三方，可能挂）
    couponService.refresh(order);         // 4. 刷新优惠券
    return OrderResult.ok(order);         // 任何一步抛异常，整个下单都失败
}`

const mqCallSnippet = `// 引入 MQ 后：下单只做核心动作，其余异步消费
public OrderResult createOrder(OrderReq req) {
    Order order = orderDao.save(req);          // 1. 核心：落库
    mq.send("order.created", order.toEvent()); // 2. 发一条"订单已创建"事件
    return OrderResult.ok(order);              // 立即返回，用户体验快
}
// 积分、短信、优惠券各自订阅 order.created，自己消费、自己重试`

const peakSnippet = `场景：秒杀开始的 1 秒内涌入 10 万请求，而数据库每秒只能稳住 2000 写入。

直连数据库：10 万请求瞬间压垮 DB，连接耗尽、雪崩。

经过 MQ 削峰：
  10 万请求  ->  全部快速写入 MQ（顺序写盘，扛得住）
                     |
                     v  消费者按 DB 能承受的速率拉取
              稳定 2000/s  ->  数据库平稳消化
  代价：用户看到"排队中/处理中"，结果稍后异步告知。`

export default function Ch1() {
  return (
    <article>
      <Lead>
        消息队列（Message Queue，简称 MQ）是后端系统里最常被问到、也最容易「会用但讲不清」的一类中间件。
        它本质上只做一件事：让一个系统把消息<strong>先放进一个队列</strong>，另一个系统稍后<strong>再从队列里取出来处理</strong>，
        发送方和接收方不必同时在线、不必互相等待。这一章我们先把「什么是消息队列、为什么需要它、典型用在哪」三个最基础也最常考的问题讲透，
        为后面所有原理与面试题打底。
      </Lead>

      <h2>一、什么是消息队列：一句话与一张图</h2>
      <p>
        最朴素的理解：消息队列就是一个<strong>架在两个系统之间的「缓冲水池」</strong>。
        上游系统（生产者）把数据写成一条条<strong>消息</strong>扔进池子，下游系统（消费者）按自己的节奏从池子里捞消息处理。
        池子本身由一个独立的服务（Broker，消息中间件服务端）来维护，负责暂存消息、保证消息不丢、按规则投递。
      </p>
      <p>
        关键在于：生产者「扔完就走」，不必等消费者处理完；消费者「忙完再捞」，不必担心来不及接。
        这种<strong>把「发」和「收」在时间上解开</strong>的能力，就是消息队列存在的根本理由。
        它和我们熟悉的同步函数调用（调了就得原地等返回）形成鲜明对比。
      </p>
      <KeyIdea>
        消息队列的核心是<strong>异步通信</strong>：生产者发送消息后立即返回，消费者在未来某个时刻独立消费。
        发送与接收在时间上被解耦，二者之间隔着一个能暂存、能缓冲、能保证可靠投递的 Broker。
      </KeyIdea>

      <h2>二、为什么需要消息队列：三大核心价值</h2>
      <p>
        面试里这道题几乎必问，标准答案是三个词：<strong>解耦、削峰、异步</strong>。但只背词不够，要能说出每个词背后的「痛在哪、怎么治」。
      </p>

      <h3>价值一：解耦（Decoupling）</h3>
      <p>
        没有 MQ 时，一个核心动作往往要直接调用一堆下游。下单成功后要加积分、发短信、刷优惠券、通知物流……
        每多一个下游，下单服务的代码里就多一行调用、多一个依赖。下游挂了，上游跟着挂；下游接口改了，上游跟着改。
        这叫<strong>强耦合</strong>。
      </p>
      <CodeBlock lang="java" title="没有 MQ：下单强耦合一堆下游" code={syncCallSnippet} />
      <p>
        引入 MQ 后，下单服务只做一件核心事——落库，然后发一条「订单已创建」的事件消息就返回。
        积分、短信、优惠券等服务各自订阅这条事件，谁需要谁去消费。下单服务<strong>根本不知道</strong>有哪些下游在听，
        新增一个下游只需要新增一个消费者，下单代码一行都不用动。这就是<strong>发布-订阅</strong>带来的解耦。
      </p>
      <CodeBlock lang="java" title="有 MQ：下单只发一条事件，下游自己订阅" code={mqCallSnippet} />

      <h3>价值二：削峰（Peak Shaving）</h3>
      <p>
        流量天然有峰谷。秒杀、抢购、整点活动会在极短时间内涌入海量请求，而数据库、下游服务的处理能力是有上限的。
        如果让洪峰直接打到数据库，瞬间就会把连接池、CPU 打满，造成雪崩。
      </p>
      <p>
        MQ 的解法是<strong>「用队列做蓄水池，把瞬时洪峰摊平成平稳水流」</strong>：所有请求先快速写进 MQ（写队列比写库快得多），
        消费者再按数据库能承受的速率匀速拉取处理。峰值被「削平」，代价是部分请求从「同步立即完成」变成「异步稍后完成」。
      </p>
      <CodeBlock lang="text" title="削峰：把瞬时洪峰摊平为平稳消费" code={peakSnippet} />

      <h3>价值三：异步（Asynchrony）</h3>
      <p>
        同步调用里，主流程必须等所有步骤都跑完才能返回，响应时间是各步骤耗时之和。
        但很多步骤其实<strong>不影响主结果、也不需要立刻完成</strong>——比如下单后发短信，晚几秒发用户毫无感知。
        把这些「非核心、可延后」的动作丢给 MQ 异步处理，主流程的响应时间就只剩核心步骤那一点，用户体验大幅提升。
      </p>
      <Example title="异步带来的响应时间差异">
        <p>
          假设下单核心落库 50ms、加积分 100ms、发短信 300ms、刷优惠券 80ms。
        </p>
        <p>
          <strong>同步串行</strong>：用户要等 50 + 100 + 300 + 80 = 530ms 才看到「下单成功」。
        </p>
        <p>
          <strong>MQ 异步</strong>：用户只等核心落库 + 发消息约 50 + 5 = 55ms 就看到成功，其余三件事在后台慢慢做。
          响应时间从 530ms 降到 55ms。
        </p>
      </Example>

      <Callout variant="note" title="解耦/削峰/异步其实是一体三面">
        三个价值常常同时发生：把下游调用改成发消息，既「解耦」了依赖、又把同步等待变成了「异步」、还顺便获得了「削峰」缓冲。
        面试时能点出「它们本质都是利用 MQ 在时间与依赖上做了一层缓冲与隔离」，比单纯背三个词更有说服力。
      </Callout>

      <h2>三、引入消息队列的代价</h2>
      <p>
        MQ 不是银弹。这道反向追问也很常见：<strong>「引入 MQ 有什么缺点？」</strong> 必须诚实回答，否则显得只会鼓吹。
      </p>
      <table>
        <thead>
          <tr><th>代价</th><th>具体表现</th></tr>
        </thead>
        <tbody>
          <tr><td>系统可用性下降</td><td>多引入一个 Broker，它一旦挂了，依赖它的链路全断；必须为 MQ 本身做高可用。</td></tr>
          <tr><td>系统复杂度上升</td><td>要额外处理消息丢失、重复消费、顺序、堆积等问题（正是本课第二卷的主题）。</td></tr>
          <tr><td>一致性问题</td><td>异步意味着「下单成功」但「积分还没加」会有一段中间态，需要最终一致性方案兜底。</td></tr>
          <tr><td>运维与排查成本</td><td>链路变长，出问题时要跨生产者、Broker、消费者三段排查，定位更难。</td></tr>
        </tbody>
      </table>

      <h2>四、典型应用场景速览</h2>
      <p>
        除了上面三大价值对应的「业务解耦、流量削峰、异步处理」，MQ 还有一批更具体的高频场景，面试时能多举几个会加分：
      </p>
      <ul>
        <li><strong>应用解耦 / 事件驱动架构</strong>：以「领域事件」为中心，各服务围绕事件协作，是微服务里的常见骨架。</li>
        <li><strong>日志收集</strong>：Kafka 最初就是为日志而生——各服务把日志写进 Kafka，再由消费者灌进 ES、数仓、监控系统。</li>
        <li><strong>流式处理 / 实时计算</strong>：把 MQ 当作流数据的管道，对接 Flink、Spark Streaming 做实时统计与风控。</li>
        <li><strong>数据同步 / CDC</strong>：把数据库 binlog 投递到 MQ，下游订阅来做缓存更新、搜索索引重建、异构数据同步。</li>
        <li><strong>分布式事务的最终一致</strong>：用「本地消息表 + MQ」或事务消息保证跨服务操作最终一致（后续章节展开）。</li>
        <li><strong>延迟任务</strong>：订单超时未支付自动取消、定时提醒等，靠 MQ 的延迟消息能力实现。</li>
      </ul>

      <h2>五、面试精讲</h2>

      <h3>Q1：请你用一两句话向非技术同学解释「什么是消息队列」</h3>
      <p>
        这道题考的不是术语，而是<strong>抽象表达能力</strong>。能把复杂概念讲给外行听，往往比堆术语更显功底。
      </p>
      <p>
        <strong>原创讲解。</strong>我会这样打比方：消息队列就像餐厅后厨那块「点单夹板」。服务员（生产者）把客人的点单写成小票夹上去就走，
        继续招待下一桌；厨师（消费者）忙完手头的菜，回头从夹板上取下一张小票来做。服务员不用站在灶台边等菜做好，厨师也不用追着服务员要单子。
        这块夹板就是队列：它<strong>暂存任务、解开了点单和做菜在时间上的强绑定</strong>。
        高峰期点单再多，夹板也能先夹着，厨师按自己的速度做——这就是削峰。能把比喻和「异步、解耦、削峰」三个词对应上，这题就满分了。
      </p>
      <p>
        <strong>易错点。</strong>别一上来就讲「分区、副本、ISR」，那是把面试官当机器讲。先讲清「是什么、解决什么」，再视追问深入。
      </p>

      <h3>Q2：消息队列带来的最大风险是什么？你会怎么对冲？</h3>
      <p>
        <strong>原创讲解。</strong>最大的风险是<strong>「系统整体可用性反而下降」</strong>——你为了治耦合，引入了一个新的、所有人都依赖的单点。
        Broker 一挂，发不出消息、消费不了消息，整条异步链路瘫痪。
      </p>
      <p>对冲手段分三层：</p>
      <ul>
        <li><strong>Broker 自身高可用</strong>：集群部署 + 多副本，挂一个节点不影响整体（这正是后面 Kafka/RocketMQ 副本机制要解决的）。</li>
        <li><strong>生产端兜底</strong>：发送失败要有重试与本地落盘（如本地消息表），不能消息一发失败就丢。</li>
        <li><strong>降级预案</strong>：MQ 整体不可用时，核心链路要能退化为同步直连或暂存本地，事后补偿，避免业务完全停摆。</li>
      </ul>
      <p>
        <strong>面试追问：</strong>「那一致性怎么办？」——异步天然只能做<strong>最终一致</strong>，要靠消息可靠投递 + 消费幂等 + 必要时的对账补偿来兜底，
        这部分是第二卷的核心，后面会细讲。
      </p>

      <h3>Q3：什么情况下「不该」用消息队列？</h3>
      <p>
        <strong>原创讲解。</strong>能说清边界，比只会鼓吹更显成熟。我会列三种不该用的情况：
      </p>
      <table>
        <thead>
          <tr><th>场景</th><th>为什么不该用 MQ</th></tr>
        </thead>
        <tbody>
          <tr><td>需要同步拿到结果</td><td>比如查询余额、校验库存后立刻返回，调用方必须当场要答案，异步只会添乱。</td></tr>
          <tr><td>强一致性事务</td><td>转账这类要求 ACID 强一致的场景，MQ 的最终一致兜不住，应走数据库事务或分布式事务协议。</td></tr>
          <tr><td>系统简单、调用链短</td><td>就两个服务、流量也不大，直接 RPC 更简单；硬上 MQ 是过度设计，徒增运维与排查成本。</td></tr>
        </tbody>
      </table>
      <p>
        <strong>面试追问：</strong>「下单后扣库存，能用 MQ 异步扣吗？」——要分情况：如果是「下单即锁定库存」的强一致场景，异步扣会超卖，应同步；
        如果库存是宽松的、允许最终一致（如预扣 + 对账），可以异步。答这种题的关键是<strong>先问清业务对一致性的要求</strong>，再给方案。
      </p>

      <h3>Q4：用 MQ 解耦后，下游处理失败了怎么办？数据不就丢了吗？</h3>
      <p>
        <strong>原创讲解。</strong>这是个很能体现「是否真正用过 MQ」的追问。答案是<strong>不会丢，但要靠机制兜底</strong>，分三层来讲：
      </p>
      <ul>
        <li><strong>消息不删除，失败可重试</strong>：消费者处理失败时不提交位点 / 不 ACK，消息仍在队列里，会被重新投递。
          只要业务做了幂等，反复重试直到成功即可。</li>
        <li><strong>重试有上限，超限进死信</strong>：不能无限重试一条「毒消息」。达到重试上限后把它转入死信队列，
          由人工或专门程序处理，避免它堵住正常消息。</li>
        <li><strong>对账补偿兜底</strong>：异步链路天然是最终一致，关键业务还应有定时对账任务，
          比对上下游状态、发现遗漏再补偿，作为最后一道防线。</li>
      </ul>
      <p>
        <strong>面试追问：</strong>「如果下游永久处理不了呢？」——那就是业务异常，死信队列 + 告警让人介入；
        MQ 的职责是「保证消息送达并可重试」，至于「业务能不能处理」要靠业务侧设计与监控，不能指望 MQ 替业务兜底。
      </p>

      <Summary
        points={[
          '消息队列是架在系统间的「缓冲水池」：生产者发消息即走，消费者按自己节奏拉取，由 Broker 暂存并可靠投递，本质是异步通信。',
          '三大核心价值：解耦（发布订阅、下游各自订阅，新增下游不改上游）、削峰（队列做蓄水池摊平洪峰）、异步（非核心动作延后做，主流程响应更快）。',
          '代价同样要记牢：可用性下降（多了个依赖单点）、复杂度上升（要处理丢失/重复/顺序/堆积）、一致性变最终一致、运维排查更难。',
          '典型场景：业务解耦/事件驱动、日志收集、流式处理、数据同步/CDC、分布式事务最终一致、延迟任务。',
          '不该用 MQ 的场景：需要同步结果、强一致事务、系统简单调用链短——能说清边界比只会鼓吹更显成熟。',
        ]}
      />
    </article>
  )
}
