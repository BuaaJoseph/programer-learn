import Lead from '@/components/cards/Lead.jsx'
import KeyIdea from '@/components/cards/KeyIdea.jsx'
import Callout from '@/components/cards/Callout.jsx'
import CodeBlock from '@/components/cards/CodeBlock.jsx'
import Example from '@/components/cards/Example.jsx'
import Summary from '@/components/cards/Summary.jsx'

const orderBreakSnippet = `// 同一订单的两条事件，发到了不同分区 -> 顺序无法保证
producer.send("order", "下单"); // 哈希后落到 partition 0
producer.send("order", "支付"); // 哈希后落到 partition 2
// 两个分区由不同消费者并行消费，"支付"可能先于"下单"被处理！

// 正解：用订单号做 key，同一订单的所有事件落到同一分区
producer.send("order", orderId, "下单"); // key=orderId -> partition 1
producer.send("order", orderId, "支付"); // key=orderId -> partition 1（同一个）`

const singleConsumerSnippet = `分区内有序的代价：

  Topic order, 3 个分区，但要严格保序就只能：
    partition 1 ---> 单线程顺序消费（一个消费者，一条一条处理）

  若想并发提速又要保序 -> 矛盾。
  折中：用业务键哈希分区，做到"同一业务键有序"，
        不同业务键之间并行，整体既快又局部有序。`

const backlogSnippet = `消息堆积的本质：生产速率 > 消费速率，持续一段时间。

  生产 5000/s
       |
       v
  [ MQ 队列 ]  堆积量 = (生产速率 - 消费速率) × 时间
       |
       v
  消费 1000/s   ->  每秒净堆积 4000 条，越积越多

泄洪三板斧：
  1. 加消费者实例（但受分区数上限制约）
  2. 提分区数 + 同步扩消费者
  3. 临时加一个"搬运"消费组，先把消息快速转存，再慢慢处理`

export default function Ch2() {
  return (
    <article>
      <Lead>
        这一章处理通用难题里剩下的两块：<strong>「如何保证消息有序性」</strong>和<strong>「如何处理消息堆积」</strong>。
        前者考的是你对「有序」与「并发」这对天生矛盾的理解——保序往往要牺牲并发；
        后者考的是线上应急能力——大量消息积压时，你能不能快速泄洪、找到根因。这两题都偏实战，答好需要既懂原理又有方案。
      </Lead>

      <h2>一、消息有序性：为什么这么难</h2>
      <p>
        「有序」听起来理所当然，但在分布式 MQ 里却是<strong>反并发</strong>的。原因在于：MQ 为了高吞吐，
        会把一个 Topic 拆成多个分区 / 队列，让多个消费者并行消费。<strong>一旦并行，全局顺序就被打散了</strong>——
        消息被分散到不同分区，不同分区由不同消费者以不同速度处理，先发的未必先被处理完。
      </p>
      <KeyIdea>
        有序性分两个层级：<strong>全局有序</strong>（整个 Topic 所有消息严格按发送顺序消费，代价是只能单分区单消费者，吞吐极低，几乎不用）；
        <strong>分区有序 / 局部有序</strong>（同一分区内严格有序，不同分区并行）。
        工程上几乎都用<strong>「业务键哈希到同一分区 + 该分区单线程消费」</strong>来实现「同一业务实体有序」，兼顾顺序与吞吐。
      </KeyIdea>

      <h3>怎么做到「同一业务有序」</h3>
      <p>
        关键两步，缺一不可：
      </p>
      <ol>
        <li><strong>发送端</strong>：用业务键（如订单号、用户 ID）作为分区 key，保证同一实体的所有消息<strong>落到同一个分区</strong>。
          不同订单可以并行，但同一订单的「下单→支付→发货」必然在一个分区里排好队。</li>
        <li><strong>消费端</strong>：该分区<strong>单线程顺序消费</strong>，处理完一条再拉下一条。
          如果在消费端又开多线程并发处理同一分区的消息，分区内的顺序还是会被打乱。</li>
      </ol>
      <CodeBlock lang="java" title="同一业务键落同一分区，保证局部有序" code={orderBreakSnippet} />
      <CodeBlock lang="text" title="分区内有序与并发提速的矛盾" code={singleConsumerSnippet} />

      <Callout variant="warn" title="重试也会破坏顺序">
        即使做到了分区有序，如果某条消息处理失败被<strong>放进重试队列稍后再试</strong>，它就跑到后面去了，顺序又乱了。
        严格保序场景里，一条卡住要么<strong>阻塞等它成功</strong>（牺牲吞吐），要么整条业务链路设计成<strong>不依赖严格顺序</strong>
        （比如用状态机让乱序也能正确收敛）。RocketMQ 的顺序消费在失败时会一直重试当前消息、阻塞后续，正是为了保序。
      </Callout>

      <Example title="一个真实的顺序陷阱">
        <p>
          电商场景，对同一商品先发「库存 +100」再发「库存 -30」。如果两条消息落到不同分区被并行消费，
          可能先执行了「-30」（此时库存还是 0，扣成负数或失败），再执行「+100」，最终库存对不上。
        </p>
        <p>
          <strong>修复</strong>：用<strong>商品 ID 做分区 key</strong>，同一商品的库存变更全进一个分区、单线程顺序消费，
          就能保证「先加后减」按发送顺序执行。不同商品之间互不影响，仍可并行。
        </p>
      </Example>

      <h2>二、消息堆积：成因与应对</h2>
      <p>
        <strong>消息堆积</strong>指消息在 Broker 里大量积压、消费跟不上。本质只有一个公式：
        <strong>生产速率持续大于消费速率</strong>。堆积危害不小——消费延迟飙升、磁盘被撑满、甚至触发 Broker 限流或拒绝写入。
      </p>
      <CodeBlock lang="text" title="堆积的本质与泄洪思路" code={backlogSnippet} />

      <h3>堆积的常见成因</h3>
      <table>
        <thead>
          <tr><th>成因</th><th>说明</th></tr>
        </thead>
        <tbody>
          <tr><td>生产突增</td><td>大促、活动、上游批量任务导致瞬时生产量远超平时。</td></tr>
          <tr><td>消费变慢</td><td>消费逻辑里有慢 SQL、慢 RPC、外部接口超时，单条处理时间被拉长。</td></tr>
          <tr><td>消费者挂了 / 重启</td><td>消费实例宕机或发布期间无人消费，消息持续积压。</td></tr>
          <tr><td>消费卡死</td><td>某条「毒消息」反复处理失败又不断重试，把消费线程占死。</td></tr>
          <tr><td>分区数不足</td><td>消费者实例数受分区数限制，分区太少导致并行度上不去。</td></tr>
        </tbody>
      </table>

      <h3>堆积后的应急处理</h3>
      <p>分两步：<strong>先止血泄洪，再排查根因</strong>。</p>
      <ul>
        <li><strong>扩容消费能力</strong>：增加消费者实例。但注意——<strong>消费者数量超过分区数无效</strong>（多出来的会空闲），
          所以可能需要先扩分区再扩消费者。</li>
        <li><strong>临时转存方案</strong>：若分区一时扩不了，可临时上一个「搬运」消费组，把积压消息<strong>快速读出后转写到一个分区更多的新 Topic</strong>，
          再用更多消费者去消费新 Topic——相当于绕开原分区数瓶颈紧急扩容。</li>
        <li><strong>优化消费逻辑</strong>：把慢操作改批量（批量查库、批量写）、异步化、加缓存，提升单实例吞吐。</li>
        <li><strong>隔离毒消息</strong>：把反复失败的消息踢到<strong>死信队列</strong>，别让它堵住正常消息。</li>
        <li><strong>必要时丢弃</strong>：若是可丢弃的日志类消息且业务允许，紧急时可跳过部分老消息（重置位点）保住实时性，事后再补。</li>
      </ul>

      <Callout variant="note" title="预防胜于救火">
        堆积治理重在事前：给消费延迟 / 堆积量配监控告警，提前发现；分区数预留一定余量；消费逻辑做好性能基线；
        发布采用滚动方式避免全部实例同时下线。等到积压几千万条再救火，往往已经造成业务影响了。
      </Callout>

      <h2>三、面试精讲</h2>

      <h3>Q1：如何保证消息的顺序性？</h3>
      <p>
        <strong>原创讲解。</strong>先把「有序」拆成两层再答，显得有层次：
      </p>
      <ul>
        <li><strong>全局有序</strong>：整个 Topic 单分区、单消费者、单线程。能做到但吞吐极低，<strong>实际几乎不用</strong>。</li>
        <li><strong>局部（分区）有序</strong>：这才是工程答案。两步——发送端用业务键哈希让<strong>同一实体消息进同一分区</strong>，
          消费端对该分区<strong>单线程顺序处理</strong>。同一订单/用户的消息严格有序，不同实体并行，兼顾顺序与吞吐。</li>
      </ul>
      <p>
        <strong>易错点。</strong>只说「用 key 哈希到一个分区」是不够的——如果消费端对该分区又开了多线程并发，顺序照样乱。
        必须强调<strong>「发送进同一分区 + 消费端该分区单线程」两个条件同时满足</strong>。
      </p>
      <p>
        <strong>面试追问：</strong>「消费失败重试会不会破坏顺序？」——会。严格保序时只能阻塞重试当前消息（牺牲吞吐与可用性），
        所以更好的做法是让业务设计成对乱序不敏感（状态机最终收敛），只在真正必须严格有序的场景才付出保序代价。
      </p>

      <h3>Q2：线上消息大量积压了，你怎么快速处理？</h3>
      <p>
        <strong>原创讲解。</strong>这是典型的应急题，面试官想听<strong>有条理的处理流程</strong>，而不是单点方案。我会这样答：
      </p>
      <ol>
        <li><strong>先定性</strong>：看监控确认堆积量、增长速率，判断是生产突增还是消费变慢/停摆。</li>
        <li><strong>先止血</strong>：如果是消费者挂了，先拉起；如果是毒消息卡死，先把它隔离到死信队列；如果是消费慢，先扩消费者实例（注意分区数上限）。</li>
        <li><strong>紧急扩容</strong>：分区不够时，临时起搬运消费组把积压转写到分区更多的新 Topic，再用大量消费者消费——绕开分区瓶颈。</li>
        <li><strong>再治本</strong>：优化消费逻辑（批量、异步、加缓存），加分区，复盘为什么会积压、补监控告警。</li>
      </ol>
      <p>
        <strong>面试追问：</strong>「为什么不能无限加消费者？」——因为<strong>消费者并行度受分区数限制</strong>：
        同一消费组内，一个分区同一时刻只能被一个消费者消费，消费者数超过分区数，多出来的就闲着。要提并行度得先加分区。
      </p>

      <h3>Q3：增加分区数有没有副作用？</h3>
      <p>
        <strong>原创讲解。</strong>有，这是个容易被忽略的追问点：
      </p>
      <table>
        <thead>
          <tr><th>副作用</th><th>说明</th></tr>
        </thead>
        <tbody>
          <tr><td>破坏既有顺序</td><td>分区数变化会改变 key 的哈希落点，加分区后同一 key 可能落到新分区，跨「加分区时刻」的顺序无法保证。</td></tr>
          <tr><td>只能加不能减</td><td>Kafka 分区数通常只支持增加，加多了想缩回去很麻烦。</td></tr>
          <tr><td>资源开销</td><td>分区越多，文件句柄、元数据、副本同步开销越大，过多分区反而拖累集群。</td></tr>
        </tbody>
      </table>
      <p>
        所以「加分区」是泄洪手段但不能滥用——对有序性敏感的 Topic 尤其要谨慎，最好<strong>一开始就按峰值预留合理分区数</strong>，
        而不是事后频繁调整。
      </p>

      <h3>Q4：既要消息有序、又要消费够快，这对矛盾怎么平衡？</h3>
      <p>
        <strong>原创讲解。</strong>这道题考的是你能否跳出「二选一」的思维，找到工程折中。我会先点明矛盾的本质：
        <strong>有序要求串行、提速要求并行，二者天生对立</strong>。然后给出三种平衡思路，按从优到次排列：
      </p>
      <ul>
        <li><strong>用业务键分流，做到「按键有序、跨键并行」</strong>：这是首选。
          同一订单/用户的消息进同一分区串行处理（局部有序），不同订单/用户落不同分区并行（整体高吞吐）。
          只要业务的有序粒度是「单实体内」，这招就能同时拿到顺序和速度。</li>
        <li><strong>消费端按 key 二次分发到内存队列</strong>：单线程从分区拉消息，再按业务键哈希分发到多个内存队列，
          每个队列一个线程处理——分区内仍按 key 有序，但多 key 之间在消费端并行。要小心位点提交与内存队列的可靠性。</li>
        <li><strong>放宽有序要求</strong>：很多业务其实不需要严格有序，只要最终状态正确。
          用<strong>状态机/版本号</strong>让乱序消息也能正确收敛（如带版本号的更新丢弃旧版本），就能彻底并行、吞吐拉满。</li>
      </ul>
      <p>
        <strong>面试追问：</strong>「第二种方案，消费端内存队列里的消息还没处理完就崩了会怎样？」——会丢，
        因为位点可能已提交但内存队列里的消息没落地。所以要么处理完整批再提交位点（崩溃后整批重放 + 幂等），
        要么给内存队列加持久化，本质又回到「可靠性 vs 性能」的权衡上。
      </p>

      <Callout variant="warn" title="堆积时最容易踩的两个坑">
        ① <strong>盲目加消费者无效</strong>：消费者数超过分区数时多出来的实例完全空闲，必须先加分区或转存到多分区新 Topic 才有用。
        ② <strong>跳过堆积消息时忘了幂等</strong>：紧急重置位点跳过老消息后，若之后又补消费这批，没有幂等就会重复处理。
        救火动作也要守住「不丢、不重」的底线，别为了泄洪制造新故障。
      </Callout>

      <Summary
        points={[
          '有序与并发天生矛盾：MQ 为高吞吐把 Topic 拆多分区并行消费，全局顺序随之被打散。',
          '全局有序代价极高（单分区单消费者）几乎不用；工程上用「业务键哈希进同一分区 + 该分区单线程消费」实现局部有序，兼顾顺序与吞吐。',
          '保序两条件缺一不可：发送进同一分区 + 消费端该分区单线程；重试会破坏顺序，严格保序需阻塞重试或让业务对乱序不敏感。',
          '堆积本质是生产速率持续大于消费速率；成因有生产突增、消费变慢、消费者挂、毒消息卡死、分区不足。',
          '堆积应急：先定性→先止血（拉起消费者/隔离毒消息/扩实例）→紧急扩容（搬运到多分区新 Topic）→治本（优化消费、加分区、补监控）。',
          '消费并行度受分区数限制，消费者超过分区数无效；加分区能泄洪但会破坏既有顺序、通常只增不减、过多有开销，应事前按峰值预留。',
        ]}
      />
    </article>
  )
}
